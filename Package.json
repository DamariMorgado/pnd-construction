{
  "name": "pnd-construction",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "validate:json": "node -e \"const fs=require('fs'); for (const file of ['package.json','package-lock.json']) { JSON.parse(fs.readFileSync(file,'utf8')); } console.log('JSON manifests valid');\""
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.1",
    "vite": "^5.2.0"
  }
}
